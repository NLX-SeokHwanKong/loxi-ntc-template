# First line is the header fields for columns and is mandatory.
# Regular expressions are supported in all fields except the first.
# Last field supports variable length command completion.
# abc[[xyz]] is expanded to abc(x(y(z)?)?)?, regexp inside [[]] is not supported
#
# Rules of Ordering:
#  - OS in alphabetical order
#  - Template name in length order (longest to shortest)
#  - When Length is the same, use alphabetical order
#  - Keep space between OS's
#
Template, Hostname, Platform, Command

linux_arp_-a.textfsm, .*, linux, arp -a

linux_ipneigh.textfsm, .*, linux, ip neigh show
linux_showsys.textfsm, .*, linux, cat /proc/uptime; hostnamectl
linux_fs.textfsm, .*, linux, df -hT
linux_ifCounters.textfsm, .*, linux, cat /proc/net/dev
linux_ipaddr.textfsm, .*, linux, ip -d address show
linux_fdb.textfsm, .*, linux, /sbin/bridge fdb show
linux_routes.textfsm, .*, linux, ip route show table all
linux_timedatectl.textfsm, .*, linux, timedatectl
linux_top.textfsm, .*, linux, top -bn 1 | head -17
linux_top.textfsm, .*, linux, top -bn 1 -o '%MEM' | head -17
linux_vlan.textfsm, .*, linux, /sbin/bridge -c vlan show


